"""Deprecated stub. Run ``python app.py`` from the repository root.

``02_src`` cannot be a Python package directly (name starts with a digit);
the root-level launcher registers it under the alias ``edaapp``.
"""


def main() -> None:
    raise SystemExit("Run `python app.py` from the repository root instead.")


if __name__ == "__main__":
    main()
